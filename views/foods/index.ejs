<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Pantry</title>
  </head>
  <body>
    <%- include('../partials/_navbar.ejs') %>
    <a href="foods/new">Add a new Pantry</a>
    <h1>Welcome to  Pantry</h1>
    <ul>
      <% foods.forEach( food => { %>
      <li>
        <a href="/users/<%= user._id %>/foods/<%= food._id %>">
          <%= food.name %> </a>
      </li>
      <% }) %>
  </body>
</html>